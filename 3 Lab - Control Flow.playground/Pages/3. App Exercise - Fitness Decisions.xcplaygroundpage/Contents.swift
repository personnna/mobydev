/*:
## App Exercise - Fitness Decisions
 
 >These exercises reinforce Swift concepts in the context of a fitness tracking app. Эти упражнения укрепляют концепции Swift в контексте приложения для отслеживания физической активности.
 
 You want your fitness tracking app to give as much encouragement as possible to your users. Create a variable `steps` equal to the number of steps you guess you've taken today. Create a constant `stepGoal` equal to 10,000. Write an if-else statement that will print "You're almost halfway there!" if `steps` is less than half of `stepGoal`, and will print "You're over halfway there!" if `steps` is greater than half of `stepGoal`.
 */
/*:
 Вы хотите, чтобы ваше приложение для отслеживания фитнеса как можно больше поощряло ваших пользователей. Создайте переменную `steps`, равную количеству шагов, которые вы сделали сегодня. Создайте константу stepGoal, равную 10 000. Напишите оператор if-else, который напечатает «Вы почти на полпути!» если `steps` меньше половины `stepGoal`, и будет напечатано "Вы уже на полпути!" если `steps` больше половины stepGoal.

 */
//:  Now create a new, but similar, if-else-if statement that prints "Way to get a good start today!" if `steps` is less than a tenth of `stepGoal`, prints "You're almost halfway there!" if `steps` is less than half of `stepGoal`, and prints "You're over halfway there!" if `steps` is greater than half of `stepGoal`.

/*:
 Теперь создайте новый, но похожий оператор if-else-if, который печатает "Как хорошо начать сегодня!" если `steps` меньше одной десятой от `stepGoal`, печатает "Вы почти на полпути!" если `steps` меньше половины `stepGoal`, и печатает "Вы уже на полпути!" если `steps` больше половины stepGoal.
 */
/*:
[Previous](@previous)  [Next: Exercise - Boolean Practice](@next)
 */
