/*:
## Exercise - Structs, Instances, and Default Values
 
 Imagine you are creating an app that will monitor location. Create a `GPS` struct with two variable properties, `latitude` and `longitude`, both with default values of 0.0.
 */
/*:
Представьте, что вы создаете приложение, которое будет отслеживать местоположение. Создайте структуру `GPS` с двумя переменными свойствами, `широта` и `долгота`, оба со значениями по умолчанию 0,0.
 */




//:  Create a variable instance of `GPS` called `somePlace`. It should be initialized without supplying any arguments. Print out the latitude and longitude of `somePlace`, which should be 0.0 for both.
/*:
 Создать экземпляр переменной `GPS` с именем `somePlace`. Он должен быть инициализирован без предоставления каких-либо аргументов. Распечатайте широту и долготу `somePlace`, которые должны быть равны 0.0 для обоих.
 */



//:  Change `somePlace`'s latitude to 51.514004, and the longitude to 0.125226, then print the updated values.
/*:
 Измените широту `somePlace` на 51,514004, а долготу на 0,125226, затем выведите обновленные значения.
 */

//:  Now imagine you are making a social app for sharing your favorite books. Create a `Book` struct with four variable properties: `title`, `author`, `pages`, and `price`. The default values for both `title` and `author` should be an empty string. `pages` should default to 0, and `price` should default to 0.0.
/*:
Теперь представьте, что вы делаете социальное приложение для обмена любимыми книгами. Создайте структуру `Book` с четырьмя переменными свойствами: `title`, `автор`, `страницы` и `цена`. Значения по умолчанию как для `title`, так и для `author` должны быть пустой строкой. `pages` по умолчанию должно быть равно 0, а `price` должно по умолчанию равно 0.0.
 */






//:  Create a variable instance of `Book` called `favoriteBook` without supplying any arguments. Print out the title of `favoriteBook`. Does it currently reflect the title of your favorite book? Probably not. Change all four properties of `favoriteBook` to reflect your favorite book. Then, using the properties of `favoriteBook`, print out facts about the book.
/*:
Создайте экземпляр переменной `Book` с именем `favoriteBook` без указания каких-либо аргументов. Распечатайте название `favoriteBook`. Отражает ли оно в настоящее время название вашей любимой книги? Возможно нет. Измените все четыре свойства `favoriteBook`, чтобы они отражали вашу любимую книгу. Затем, используя свойства `favoriteBook`, распечатайте факты о книге.
 */




/*:
page 1 of 10  |  [Next: App Exercise - Workout Tracking](@next)
 */
